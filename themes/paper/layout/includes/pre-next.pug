.pre-next
  if(page.prev)
    a.pre-button(href=url_for(page.prev.path))= page.prev.title
  else
    a.pre-button No More

  if(page.next)
    a.next-button(href=url_for(page.next.path))= page.next.title
  else
    a.next-button No More
    